मान f१;
मान f२;
मान f३;

चक्र (मान i = १; i < ४; i = i + १) {
  मान j = i;
  सूत्र f() {
    वद(i);
    वद(j);
  }

  यदि (j == १) f१ = f;
  अथ यदि (j == २) f२ = f;
  अथ f३ = f;
}

f१(); # expect: ४
      # expect: १
f२(); # expect: ४
      # expect: २
f३(); # expect: ४
      # expect: ३
