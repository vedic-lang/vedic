विधि Base {
  प्रारंभ(a, b) {
    वद("Base.प्रारंभ(" + a + ", " + b + ")");
  }
}

विधि Derived सन्धि Base {
  प्रारंभ() {
    वद("Derived.प्रारंभ()");
    मित्र.प्रारंभ("a", "b");
  }
}

Derived();
# expect: Derived.प्रारंभ()
# expect: Base.प्रारंभ(a, b)
